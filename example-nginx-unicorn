server {
    listen 3000;
    server_name example.org;

    location / {
        #For unicorn
        proxy_pass http://source:8010;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
